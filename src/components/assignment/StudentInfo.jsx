// Change these two values ONCE and they update in every question's output.
const MY_NAME = "Aaditya Adhikari";
const MY_ROLL_NO = "5";

const StudentInfo = () => {
  return (
    <p style={{ fontWeight: "bold", color: "#555" }}>
      Name: {MY_NAME} | Roll No: {MY_ROLL_NO}
    </p>
  );
};

export default StudentInfo;
